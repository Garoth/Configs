[user]
	name = Andrei Thorp
	email = athorp@evernote.com

[color]
	ui = true

[push]
	default = tracking

[alias]
	st = status
	l = log
	ci = commit
	co = checkout
	gerrit = push origin HEAD:refs/for/master

[receive]
	denyCurrentBranch = warn
[gerrit]
	reviewer = athorp@evernote.com
	user = athorp
[core]
	pager = "vim -V0 -M -c 'if line2byte(line(\"$\") +1) == 2 | q! | endif' -c 'nmap q :q!<cr>' -c AnsiEsc -"
